# 2026-08-16 工作日志

## 11:44 Function Calling 设计文档
- 生成 Context_Engineering/function_call_design.md：涵盖 FC 概念、6 条设计原则（单一职责/命名/描述/参数/返回值/幂等）、JSON Schema 定义方法、完整调用循环、7 个常见坑
- 文档基于用户的 get_weather_data 天气查询场景编写

## 12:45 Function Calling 代码完善
- 重写 Context_Engineering/function_call.py：修复参数名不一致（location→city）、删除教程碎片代码、补全完整调用循环
- 实现多轮工具调用 while 循环、模块级注册映射、工具描述三要素
- 验证通过：北京天气查询返回"小雨 23-28℃"

## 17:45 Function Calling 多轮对话改造
- 将 function_call.py 从单次调用改为会话式多轮：messages 跨轮次持久化，拆出 run_tool_loop（单轮工具循环）+ chat（交互主循环）
- 关键修正：不调工具的 assistant 回复也要 append 进历史（原代码丢失 assistant 消息）
- 遇到 Moonshot 3 RPM 严格限流：SDK 内置重试无效，因为被拒请求也计入 RPM 配额；改为固定 25s 间隔手动重试（interval >= 60/RPM）
- 验证通过：三轮对话，第三轮"哪个城市更热"凭历史数据直接作答，未调工具

## 17:48 multi_function_call.py 多工具链式调用
- 修复缺失的 available_functions 注册、空 __main__、get_plane_number 无 KeyError 保护
- 航班场景实现串行依赖多工具：get_plane_number 查航班号 → 模型将结果作为参数 → get_plane_ticket_price 查票价，3 次模型请求经 while 循环完成
- 工具描述中显式引导依赖关系（"只提供路线时先调 get_plane_number"），帮助模型规划调用顺序
- 验证通过：郑州→北京 2025-07-30 返回 CZ1123 / 668 元

## 21:30 CrewAI 多智能体 agents.py + tools.py 完善
- 安装依赖：crewai + langchain + langchain-openai（managed venv）
- tools.py: 从 @tool 装饰器改为 BaseTool 子类（@tool 与 LiteLLM 存在 "function name cannot be empty" 兼容问题）；书信路径改为绝对路径 output/letter.txt；邮件配置移至 .env；SMTP_SSL 改用 with 语句
- agents.py: MoonshotChat（已废弃）→ crewai.LLM（LiteLLM 封装，model 前缀 "openai/"）；kimi-k2.6 是推理模型只允许 temperature=1，去掉了 temperature 参数
- .env 新增 EMAIL_FROM_ADDR / EMAIL_FROM_NAME / EMAIL_PWD / EMAIL_TO_ADDR / EMAIL_SMTP_SERVER / EMAIL_SUBJECT（用户需填真实值才能发邮件）
- 验证通过：作家写情书 → 编辑调工具存盘 → 寄信人调工具发邮件（因 .env 邮箱配置为空，正确返回配置缺失提示）
